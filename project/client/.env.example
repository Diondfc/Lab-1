# Dev: leave unset so /api is proxied to the Express server (see vite.config.js).
# Production or opening dist without a combined host: point the browser at the API origin.
# VITE_API_URL=http://localhost:5001

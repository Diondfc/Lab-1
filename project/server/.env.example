PORT=5001
# Comma-separated if you use multiple dev ports (Vite default 5173, npm start uses 3000)
CLIENT_ORIGIN=http://localhost:5173,http://localhost:5174,http://localhost:3000

JWT_SECRET=change-me-in-production
# Optional override (default 7d)
# JWT_EXPIRES_IN=7d

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=ubt_library
